# Add a page

1. Create a `.astro` file in `src/pages/`.
2. Reuse `Layout.astro` or the existing widget components before creating new markup.
3. Define page metadata and preserve the site's canonical URL and structured-data conventions.
4. Run `npm run build && npm run check`, then inspect the new route in light/dark mode and at mobile width.
